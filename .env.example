# Copy to .env and customize as needed. Do NOT commit your .env.

# Flask session/flash signing key (required). Use a long random value.
# Generate: openssl rand -hex 32
SECRET_KEY=

# Optional: override default highlights base path inside the container
# HIGHLIGHTS_BASE_PATH=/data/highlights

# Optional: Open Library polite identification (you can also set in Config UI)
# OL_APP_NAME=KoReaderHighlightsApp
# OL_CONTACT_EMAIL=user@example.com

# Database and broker are provided by docker-compose. Only set if running locally without compose.
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/highlights
# RABBITMQ_URL=amqp://guest:guest@localhost:5672//

